# Kite Agent 会话工作规范

## 当前职责

本工作区现在接管 Kite 主线中的 Agent 会话能力，主要负责：

- Agent 会话页面、会话列表、项目组织和用户可见消息时间线。
- 模型、推理强度、权限、Provider、Skill、MCP 和官方账号等 Agent 设置界面。
- Codex、Claude Code、OpenCode、Hermes、OpenClaw、Kimi Code、MiMo Code 等工具的兼容研究与适配。

普通 UI 调整默认只修改显示与交互，不顺手改变安装、运行通道、进程、资源卡片或其他底层机制。确实需要跨层时，先查清事实拥有者和现有合同，再单独定义任务边界。

## 架构边界

- UI 和 Kite Agent SDK 使用统一语义；具体 Agent 的差异留在注册信息、ACP 或专用 Adapter 中。
- 有 ACP 就优先使用 ACP；ACP 不足时走专用 Adapter，但仍必须经过 Kite Agent SDK，不能把第三方协议直接写进 UI。
- 显示名称只用于 Kite 界面；稳定 ID、模型 ID、会话 ID 和运行 ID 才能用于引用、恢复和请求。
- 页面提交用户意图、显示状态；状态拥有者负责持久化并发出信号。不要在页面复制第二份长期事实。
- 不为某个 Agent、当前截图、当前设备或单个测试写死特判。修复通用合同或在适配层明确声明能力。

## 需求与交流习惯

- 用户贴截图、描述问题、补充方向或询问看法，只代表讨论与确认，不自动授权修改。
- 只有用户明确说“干活”“开始改”“按这个做”等，才进入实施。
- UI 需求先用简短文字复述结构和交互；用户确认后再改，避免一边讨论一边改错方向。
- 解释面向用户，优先讲哪个模块负责什么、用户会看到什么；非必要不堆代码级名词。
- 默认使用中文；代码名、协议字段、命令和日志原文可以保留英文。

## UI 习惯

- Agent 会话界面以用户提供的 ChatGPT 移动端参考为主要视觉基准：克制、中性、留白充分、层级清楚。
- 不使用青蓝色强调文字或 Kite 旧主题色代替 ChatGPT 风格；不直接采用原生 Android 对话框作为正式页面效果。
- 模型、权限、推理强度是固定统一入口；各 Agent 能提供哪些选项由能力目录决定。
- 普通状态变化只更新相关控件，不整页刷新、不清空重建、不改变滚动位置。
- 页面绘制和列表绑定期间不得扫描文件、读取原生配置、探测进程或执行网络健康检查。
- 点击后先给出明确处理中状态，后台再确认成功、失败、继续等待或超时；不能无提示恢复原状。
- 用户负责细致视觉与多步点击验收；Codex 仍须先完成相关测试、Debug 构建、安装、冷启动和崩溃检查，不能把基础验证全部交给用户。

## Agent 配置习惯

- 默认配置与当前会话配置是两个作用域，但必须来自同一能力目录。
- Provider 的真实连接事实包括 URL、凭据和模型 ID；推理强度属于模型/会话能力，权限属于 Agent 安全策略。
- 免费或官方模型可以拥有 Kite 显示名称，但真实模型 ID 和系统来源保持只读。
- Skill、MCP 和原生配置以 Agent 的真实目录/配置为事实源；Kite 只提供统一管理入口和必要的显示偏好。

## 版本与发布规则

- 所有 AI 在判断或修改版本前，必须先读取 [版本与发布规则](docs/guides/versioning-and-releases.md)，并核对最新正式标签与 `app/build.gradle` 的真实值。
- `versionName` 使用三段式产品语义版本：新增用户可见能力升中间位并把末位归零；只有修复、兼容和性能改进时升末位；不得把提交数量、日期或开发进度写进版本号。
- 一个尚未发布的版本周期只确定一次目标版本；周期内继续增加计划功能和修复 Bug，不逐提交重复升版。正式发布后，下一轮新功能再升中间位，纯修复升末位。
- `versionCode` 是 Android 独立递增序号。每个对外分发的 APK 都必须高于此前已分发版本；它不从 `versionName` 计算，也不与资源商店 `revision`、Rootfs 或第三方资源版本共用。
- 多种改动同时存在时采用影响最高的一类。预发布使用 `-alpha.N`、`-beta.N`、`-rc.N`；每个实际分发的预发布包仍须使用新的 `versionCode`。
- 未经用户明确授权，不得修改版本、创建标签或发布；获得授权后也必须先完成版本一致性、构建产物和发布边界检查。

## Git 习惯

- 每个任务开始前先检查当前工作树、分支、HEAD、未提交文件和远端关系，再决定实施边界。
- 每个独立阶段完成后创建边界清楚的本地提交。即使视觉验收留到下一轮，也先提交已经通过自动验证的实现；验收修正另做提交。
- 不把多个不相关任务混成一个提交，不覆盖或回滚用户已有改动。
- 未经明确授权不得 push、发布、改版本、开 PR、删除远端或执行不可逆外部操作。
- 不提交缓存、构建目录、APK/AAB、本机配置、设备状态、截图、日志、临时报告或密钥。
- 长期任务可以使用 `docs/autonomous-task/` 三件套；任务结束后把稳定结论写入正式文档，并清理临时流水。

## 真机与性能

默认 Android 验收设备为魅族 18：

```text
serial: 181QGEYH222B9
model: MEIZU_18
```

- 用户可见改动能上真机就上真机；安装和启动成功不等于视觉验收完成。
- 运行、资源和 Agent 页面应优先消费已有状态与缓存，避免在主线程进行磁盘、进程、数据库或网络重活。
- 状态信号表示“相关事实变化”，不等于“重建整个页面”。
